
作业里面包含了计算器的源码 calculate2.py

很早以前写了一个，几乎没用正则，全靠列表来实现的，现在这个完全用正则改写了，除去注释用不到40行代码写了一个正则表达式版。

给定一个表达式，传入函数就可以计算结果，这里为了方便看计算结果是否正确，直接在代码里面里给出了需要
计算的表达式（s = '1 - 2 * ( (60-30 +(-40/5) * (9-2*5/3 + 7 /3*99/4*2998 +10 * 568/14 )) - (-4*3)/ (16-3*2) )'）
然后分别用eval函数计算结果和自己写的函数的出的结果进行对比，运行calculate2.py，可以看出计算的结果都是正确的

博客地址 http://www.cnblogs.com/Wxtrkbc/p/5453349.html 
由于博客很早就写了，当时只对以前的写的计算器进行了描述，对于calculate2.py，只是在后面经行了补充说明。